buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:0.8.3'
    }
}

allprojects {
    repositories {
        mavenCentral()
    }
}

task runDebug(type:Exec) {
    workingDir '.'
    commandLine './run_debug'
}
